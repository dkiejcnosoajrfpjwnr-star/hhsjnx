# بوت النشر التلقائي
بوت تليجرام لإدارة النشر في المجموعات عبر الحسابات الشخصية.

## التثبيت
1. قم بتثبيت المكتبات:
   `pip install -r requirements.txt`
2. أضف الـ API_ID والـ API_HASH الخاص بك في الكود.
3. شغل البوت:
   `python main.py`

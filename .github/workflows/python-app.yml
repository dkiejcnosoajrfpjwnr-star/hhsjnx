# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Run Telegram Bot

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # يضيف زر "تشغيل يدوياً" في صفحة الـ Actions

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # تثبيت الأدوات الضرورية لتجميع مكتبات الصوت (tgcalls)
        sudo apt-get update
        sudo apt-get install -y build-essential ffmpeg libffi-dev libssl-dev
        # تثبيت المكتبات من الملف
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: Run Bot
      run: |
        # تشغيل ملف البوت الخاص بك
        python main.py
